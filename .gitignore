# folders
__pycache__
.idea
model_data
src/physiotrack.egg-info
examples/output
# downloaded model weights at repo root (leading slash => NOT src/physiotrack)
/physiotrack/
# dataset / generated evaluation outputs
examples/CVSSP3D/
examples/cananon_evaluation_outputs/
examples/*/results/
# local research: thesis validation scripts, datasets and their outputs (never committed)
research/
docs/paper/
# built MkDocs site (published to the gh-pages branch by CI, not tracked on main)
site/

# model files
*.pt
*.engine
*.onnx
*.pth
*.bin
*.pt2

# scripts
test.py
main.py

# media
*.avi
*.mp4
*.wav
*.json
!.releaserc.json
# test fixtures (small reference data checked by the test suite)
!tests/data/*.json
# The short, synthetic tracking clip is an intentional example input.
!examples/face_tracking/data/students_face_tracking.mp4

# data / arrays
*.npz
*.npy
*.csv

# binaries / fonts / archives / shortcuts
*.dll
*.bz2
*.ttf
# ...except the bundled overlay fonts, which ship as package data
!src/physiotrack/assets/fonts/*.ttf
*.lnk

# generated docs / diagrams / scratch
*.html
*.svg
*.pdf

# duplicates / backups
* - Copy.*
*_old.*

# other
*.ovpn
